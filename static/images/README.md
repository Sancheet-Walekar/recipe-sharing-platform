# static/images

Site images that are part of the project itself (for example the
`placeholder.svg` shown when a recipe has no photo) live here.

Photos uploaded by users are **not** stored here. They go to
`static/uploads/recipes/`, which is ignored by Git.
